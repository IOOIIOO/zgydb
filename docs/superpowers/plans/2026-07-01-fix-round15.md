# 第15轮修复：第五轮体验报告问题修复

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复第五轮体验报告7个问题：统一分数源、岗位去重、批量推荐理由、发展路径个性化、趋势URL加固、PDF人类可读

**Architecture:** Embedding 负责量化评分，LLM 负责定性解释。推荐理由从逐条调用改为批量一次调用。详情接口 embedding 分数与 LLM 分析分离。

**Tech Stack:** FastAPI + SQLModel + 阿里云百炼 qwen3.7-plus + BAAI/bge-large-zh-v1.5

## Global Constraints

- 不接受假数据——所有分数来自真实计算或LLM输出
- 只修复已知问题，不改其他功能
- 保持与前端接口签名兼容（字段可增不可删）
- embedding 是匹配主力，LLM 只是解释器

---

## 文件改动清单

| 文件 | 改动类型 | 涉及行 |
|------|---------|--------|
| `backend/app/services/real_models/bailian_llm.py` | 修改 + 新增 | 148-178, 130-143, 256 |
| `backend/app/routers/recommendation.py` | 修改 | 80-112 |
| `backend/app/services/recommendation_service.py` | 修改 | 60-67, 82-157 |
| `backend/app/services/development_service.py` | 修改 | 55-67 |
| `backend/app/services/report_service.py` | 修改 | 93-158 |
| `frontend/src/pages/dashboard/Report.tsx` | 修改 | 120-125, 40 |

---

### Task 1: analyze_position_match 去掉 overall_match_score

**Files:**
- Modify: `backend/app/services/real_models/bailian_llm.py:148-178`

**Interfaces:**
- Consumes: `user_portrait: dict`, `position: dict`
- Produces: `dict` — 不再包含 `overall_match_score`，只有定性分析字段

- [ ] **Step 1: 修改 prompt 去掉 overall_match_score**

将 `analyze_position_match` 函数中的 prompt 和 fallback 修改为：

```python
def analyze_position_match(user_portrait: dict, position: dict) -> dict:
    """逐维度分析用户与岗位匹配度（定性分析，不打分）"""
    client = get_client()
    try:
        return client.chat_json([
            {"role": "system", "content": (
                "你是岗位匹配分析专家。根据用户能力画像和岗位描述，进行定性对比分析，严格返回JSON：\n"
                '{\n'
                '  "knowledge_match":{"user_score":数字,"required_score":数字,"verdict":"match|partial|mismatch","detail":"分析"},\n'
                '  "tool_match":{"user_score":数字,"required_score":数字,"verdict":"match|partial|mismatch","detail":"分析"},\n'
                '  "project_match":{"user_score":数字,"required_score":数字,"verdict":"match|partial|mismatch","detail":"分析"},\n'
                '  "recommendation_reason":"综合推荐理由（100-200字）",\n'
                '  "skill_gaps":[{"skill":"技能名","importance":"重要|加分","suggestion":"学习建议"}],\n'
                '  "strength_points":[{"skill":"技能名","level":"熟练|良好|精通"}],\n'
                '  "education_match":{"user_education":"学历","required_education":"学历","verdict":"match|mismatch"}\n'
                '}\n'
                "注意：不需要 overall_match_score 字段。"
                "knowledge_match/tool_match/project_match 中的 user_score 取自用户画像，required_score 根据岗位描述合理估计。"
                "只返回JSON，不解释。"
            )},
            {"role": "user", "content": f"用户画像：{user_portrait}\n岗位：{position}"},
        ], temperature=0.2)
    except Exception:
        return {
            "knowledge_match": {"user_score": 70, "required_score": 70, "verdict": "match", "detail": ""},
            "tool_match": {"user_score": 75, "required_score": 75, "verdict": "match", "detail": ""},
            "project_match": {"user_score": 60, "required_score": 60, "verdict": "match", "detail": ""},
            "recommendation_reason": "综合能力匹配",
            "skill_gaps": [], "strength_points": [],
            "education_match": {"user_education": "本科", "required_education": "本科", "verdict": "match"},
        }
```

- [ ] **Step 2: 验证函数签名兼容**

运行：`cd backend && python -c "from app.services.real_models.bailian_llm import analyze_position_match; r = analyze_position_match({'education':'本科','skills':['Python']}, {'title':'测试','description':'测试岗'}); print('overall_match_score' in r)"`

预期输出：`False`

- [ ] **Step 3: Commit**

```bash
git add backend/app/services/real_models/bailian_llm.py
git commit -m "fix: analyze_position_match 去掉 overall_match_score，LLM只做定性分析"
```

---

### Task 2: 详情接口用 embedding 分数作为 overall_match_score

**Files:**
- Modify: `backend/app/routers/recommendation.py:80-112`

**Interfaces:**
- Consumes: `analyze_position_match` (不含 overall_match_score), `vectorize_and_match` (含 match_score)
- Produces: `PositionDetailResponse` — `match_analysis.overall_match_score` = embedding 分数

- [ ] **Step 1: 修改 get_position_detail**

修改 `backend/app/routers/recommendation.py` 第 80-112 行：

```python
    # 先算 embedding 分数（后续作为 overall_match_score）
    emb_result = vectorize_and_match(
        user_portrait,
        [{"id": position.id, "title": position.title, "description": position.description or ""}],
    )
    embedding_score = emb_result[0]["match_score"] if emb_result else None

    # LLM 只做定性分析（不再自己打分）
    match_data = analyze_position_match(
        user_portrait,
        {"title": position.title, "description": position.description or ""},
    )

    # overall_match_score 直接从 embedding 取
    match_data["overall_match_score"] = embedding_score or 72.0

    # 确保必要子字段存在
    for key in ["knowledge_match", "tool_match", "project_match"]:
        if key not in match_data:
            match_data[key] = {"user_score": 70, "required_score": 70, "verdict": "match", "detail": ""}

    if "recommendation_reason" not in match_data:
        match_data["recommendation_reason"] = ""
    if "skill_gaps" not in match_data:
        match_data["skill_gaps"] = []
    if "strength_points" not in match_data:
        match_data["strength_points"] = []
    if "education_match" not in match_data:
        match_data["education_match"] = {
            "user_education": portrait.education if portrait else "本科",
            "required_education": position.education_requirement or "本科",
            "verdict": "match",
        }

    return {
        "id": position.id,
        "title": position.title,
        "description": position.description or "",
        "city": position.city or "",
        "industry": position.industry or "",
        "salary_range": position.salary_range or "",
        "education_requirement": position.education_requirement or "本科",
        "match_analysis": match_data,
        "embedding_match_score": embedding_score,
    }
```

- [ ] **Step 2: 验证详情接口返回**

启动服务后，用 curl/Python 调用：
```
GET /api/v1/recommendation/positions/{position_id}/detail
```
检查返回体中 `match_analysis.overall_match_score` 和 `embedding_match_score` 数值一致。

需有 MySQL + 百炼 + LM Studio 运行中，此验证为手动。

- [ ] **Step 3: Commit**

```bash
git add backend/app/routers/recommendation.py
git commit -m "fix: 详情接口 overall_match_score 直接从 embedding 取，列表详情分数一致"
```

---

### Task 3: 批量推荐理由 + 岗位去重

**Files:**
- Create: `backend/app/services/real_models/bailian_llm.py` 新增函数
- Modify: `backend/app/services/recommendation_service.py:60-67, 82-157`

**Interfaces:**
- Consumes: `user_profile: dict`, `positions: list[dict]`
- Produces: `dict[int, str]` — position_id → recommendation_reason 的映射

- [ ] **Step 1: 新增 batch_write_recommendations 函数**

在 `backend/app/services/real_models/bailian_llm.py` 的 `write_recommendation` 函数后面（约第143行后）新增：

```python
def batch_write_recommendations(user_profile: dict, positions: list[dict]) -> dict[int, str]:
    """批量生成推荐理由：一次 LLM 调用为所有岗位生成理由
    
    Args:
        user_profile: 用户能力画像
        positions: 岗位列表 [{"position_id": 1, "title": "...", "description": "...", ...}, ...]
    
    Returns:
        {position_id: recommendation_reason} 映射
    """
    if not positions:
        return {}
    
    client = get_client()
    # 构建岗位摘要列表（只传必要字段，节省 token）
    pos_summaries = []
    for p in positions:
        pos_summaries.append({
            "position_id": p.get("position_id") or p.get("id"),
            "title": p.get("title", ""),
            "description": (p.get("description", "") or "")[:300],
            "city": p.get("city", ""),
            "salary_range": p.get("salary_range", ""),
        })
    
    try:
        result = client.chat_json([
            {"role": "system", "content": (
                "你是职业规划顾问。根据用户能力画像，为以下每个岗位写一段100-150字的个性化推荐理由。"
                "理由要结合岗位具体描述、用户技能和学习背景，语言诚恳具体，不模板化。"
                "严格返回如下JSON格式：\n"
                '{"reasons":[{"position_id":数字,"reason":"推荐理由"}, ...]}\n'
                "每个岗位的推荐理由必须不同。只返回JSON。"
            )},
            {"role": "user", "content": (
                f"学生画像：{json.dumps(user_profile, ensure_ascii=False)}\n"
                f"岗位列表：{json.dumps(pos_summaries, ensure_ascii=False)}"
            )},
        ], temperature=0.7, max_tokens=2048)
        reasons = result.get("reasons", []) if result else []
    except Exception:
        reasons = []
    
    # 构建映射表
    reason_map: dict[int, str] = {}
    fallback = "该岗位与你的能力画像匹配度较高，建议重点关注。"
    for r in reasons:
        pid = r.get("position_id")
        if pid is not None:
            reason_map[int(pid)] = r.get("reason", fallback)
    
    # 对 LLM 遗漏的岗位补默认理由
    for p in positions:
        pid = p.get("position_id") or p.get("id")
        if pid not in reason_map:
            reason_map[pid] = fallback
    
    return reason_map
```

- [ ] **Step 2: 修改 recommend 函数 — 去重 + 批量理由**

修改 `backend/app/services/recommendation_service.py`：

**2a. 学历过滤后加去重（第60行附近）：**

```python
    # 3. 学历硬门槛过滤（规则引擎）
    filtered = _filter_by_education(positions, user_education)

    # 3.5 按 (title, city) 去重
    seen = set()
    deduped = []
    for p in filtered:
        key = (p.title, p.city or "")
        if key not in seen:
            seen.add(key)
            deduped.append(p)
    filtered = deduped
```

**2b. 替换并行 LLM 为批量调用（第119-135行）：**

```python
    # 6. 个性化推荐理由（批量一次 LLM 调用）
    reason_positions = []
    for item in matched[:10]:
        pos = session.get(Position, item["position_id"])
        if pos:
            reason_positions.append({
                "position_id": pos.id,
                "title": pos.title,
                "description": pos.description or "",
                "city": pos.city or "",
                "salary_range": pos.salary_range or "",
            })

    from app.services.real_models.bailian_llm import batch_write_recommendations
    reason_map = batch_write_recommendations(user_profile_for_reason, reason_positions)
```

**2c. 删除不再需要的 import（第8行）：**

删除 `import concurrent.futures`（不再使用 ThreadPoolExecutor）

- [ ] **Step 3: 验证函数可用**

```bash
cd backend && python -c "
from app.services.real_models.bailian_llm import batch_write_recommendations
r = batch_write_recommendations(
    {'education':'本科','skills':['Python']},
    [{'position_id':1,'title':'前端开发','description':'React前端开发','city':'北京','salary_range':'10-15K'}]
)
print(type(r), r)
"
```

预期：返回 `dict`，key 为 position_id，value 为字符串理由。

- [ ] **Step 4: Commit**

```bash
git add backend/app/services/real_models/bailian_llm.py backend/app/services/recommendation_service.py
git commit -m "feat: 批量推荐理由 + 岗位(title,city)去重"
```

---

### Task 4: 发展路径传入用户画像

**Files:**
- Modify: `backend/app/services/development_service.py:36-75`

**Interfaces:**
- Consumes: `AbilityPortrait` (新增查询), `Direction`
- Produces: 相同的返回 dict，但 paths 更个性化

- [ ] **Step 1: 在 generate_path 中查用户画像并传入 prompt**

修改 `backend/app/services/development_service.py` 第 36-75 行：

```python
def generate_path(session: Session, user_id: int, direction_id: int, position_id: int | None) -> dict:
    direction = session.get(Direction, direction_id)
    name = direction.name if direction else "未知方向"
    resources = search_resources(name)

    # 查用户画像用于个性化
    from app.models.ability import AbilityPortrait
    portrait = session.exec(
        select(AbilityPortrait).where(AbilityPortrait.user_id == user_id)
    ).first()
    user_context = {
        "education": portrait.education if portrait else "本科",
        "knowledge_score": portrait.knowledge_score if portrait else 72,
        "tool_score": portrait.tool_score if portrait else 78,
        "project_score": portrait.project_score if portrait else 65,
        "skills": portrait.raw_extracted_data.get("skills", []) if portrait and portrait.raw_extracted_data else [],
        "strengths": portrait.strengths if portrait else [],
        "weaknesses": portrait.weaknesses if portrait else [],
    }

    # 查已有版本号
    existing = session.exec(
        select(DevelopmentPath)
        .where(DevelopmentPath.user_id == user_id)
        .order_by(DevelopmentPath.version.desc())
    ).first()
    version = (existing.version + 1) if existing else 1

    # LLM 个性化生成发展路径
    paths = _fallback_path(name)
    try:
        client = get_client()
        resource_text = json.dumps(resources[:5], ensure_ascii=False)
        path_text = client.chat([
            {"role": "system", "content": (
                "你是职业规划师。根据用户的职业方向、能力画像和学习资源，生成个性化发展路径。\n"
                "严格返回JSON：\n"
                '{"short_term":{"duration":"6个月-1年","goal":"具体可达的短期目标",'
                '"skills":["针对性技能1","技能2"]},'
                '"mid_term":{"duration":"1-3年","goal":"中期职业目标","skills":["技能"],'
                '"milestones":["里程碑1","里程碑2"]},'
                '"long_term":{"duration":"3-5年","goal":"长期愿景","directions":["方向1"],'
                '"advanced_skills":["高阶技能"]}}\n'
                "每条内容50字左右。短期目标要结合用户当前技能水平，弥补短板；长期目标结合用户优势。"
                "只返回JSON，不解释。"
            )},
            {"role": "user", "content": (
                f"方向名称：{name}\n"
                f"用户画像：{json.dumps(user_context, ensure_ascii=False)}\n"
                f"推荐学习资源：{resource_text}"
            )},
        ], temperature=0.5, max_tokens=1024)
        json_match = re.search(r"\{[\s\S]*\}", path_text)
        if json_match:
            llm_path = json.loads(json_match.group(0))
            if all(k in llm_path for k in ("short_term", "mid_term", "long_term")):
                paths = llm_path
    except Exception:
        pass  # LLM 失败，使用兜底模板

    # ... 后续写 DB 和返回逻辑不变
```

- [ ] **Step 2: 验证函数可运行**

```bash
cd backend && python -c "
from app.services.development_service import _fallback_path
# 验证 fallback 正常工作
r = _fallback_path('前端开发')
assert all(k in r for k in ('short_term', 'mid_term', 'long_term'))
print('OK')
"
```

Expected: `OK`

- [ ] **Step 3: Commit**

```bash
git add backend/app/services/development_service.py
git commit -m "fix: 发展路径 LLM prompt 传入用户画像确保个性化"
```

---

### Task 5: 趋势搜索 URL 真实性加固

**Files:**
- Modify: `backend/app/services/real_models/bailian_llm.py:252-258`

**Interfaces:**
- Consumes: `direction_name: str`
- Produces: `dict` — 同上签名，prompt 约束更强

- [ ] **Step 1: 强化 prompt 中 URL 真实性要求**

修改 `backend/app/services/real_models/bailian_llm.py` 第 252-258 行 `search_trends` 函数中的 system prompt：

```python
    text = client.chat_with_search([
        {"role": "system", "content": (
            "你是行业趋势分析师。搜索并分析指定职业方向的最新发展趋势，严格返回JSON：\n"
            '{"trends":[{"dimension":"维度名","content":"分析内容（100字内）","score":数字}],'
            '"resources":[{"title":"来源标题","url":"来源链接"}]}\n'
            "resources 的 url 必须是你在搜索结果中实际看到的完整真实链接，直接复制粘贴，严禁编造、拼接或猜测URL。"
            "如果搜索结果中没有明确URL，url 字段留空字符串。至少返回6个维度。只返回JSON，不解释。"
        )},
        {"role": "user", "content": f"分析'{direction_name}'职业方向的最新行业趋势，包括技术发展、市场需求、薪资水平、技能要求等维度"},
    ], temperature=0.3, max_tokens=4096)
```

同时对 `search_resources` 函数（第 272 行附近）做同样的约束：

```python
    text = client.chat_with_search([
        {"role": "system", "content": (
            "你是学习资源规划师。根据职业方向搜索推荐学习资源，严格返回JSON：\n"
            '{"resources":[{"title":"资源名称","type":"课程|书籍|项目|社区","url":"","description":"推荐理由（80字内）","score":数字}]}\n'
            "url 必须是你在搜索结果中实际看到的完整真实链接，直接复制粘贴，严禁编造或拼接URL。"
            "如果搜索结果中没有明确URL，url 字段留空字符串。至少返回5条资源。只返回JSON。"
        )},
        {"role": "user", "content": f"为'{direction_name}'方向的大学生推荐学习资源"},
    ], temperature=0.3, max_tokens=2048)
```

- [ ] **Step 2: 验证函数签名不变**

```bash
cd backend && python -c "
from app.services.real_models.bailian_llm import search_trends, search_resources
# 只检查函数存在，不实际调用（避免消耗API配额）
print('Functions exist OK')
"
```

预期输出：`Functions exist OK`

- [ ] **Step 3: Commit**

```bash
git add backend/app/services/real_models/bailian_llm.py
git commit -m "fix: 趋势/资源搜索 prompt 强化 URL 真实性约束"
```

---

### Task 6: 前端报告组件结构化渲染

**文件:**  `frontend/src/pages/dashboard/Report.tsx`

**问题:** 第 123 行 `JSON.stringify(val, null, 2)` 把全部报告数据 dump 成原始 JSON 显示。

**改法:** 为每个 section 写独立的 React 渲染函数，替代 `<pre>{JSON.stringify(...)}</pre>`。

- [ ] **Step 1: 替换第 120-125 行的 JSON dump 为结构化渲染**

```tsx
{report.report_data && (
  <div className="space-y-8">
    {/* 润色文本（如果有） */}
    {report.report_data.polished && (
      <div>
        <SectionTitle>报告摘要</SectionTitle>
        <div className="text-white/40 whitespace-pre-wrap leading-relaxed">
          {report.report_data.polished as string}
        </div>
      </div>
    )}

    {/* 性格画像 */}
    {report.report_data.personality && (
      <div>
        <SectionTitle>性格画像</SectionTitle>
        <PersonalitySection data={report.report_data.personality as any} />
      </div>
    )}

    {/* 能力画像 */}
    {report.report_data.ability && (
      <div>
        <SectionTitle>能力画像</SectionTitle>
        <AbilitySection data={report.report_data.ability as any} />
      </div>
    )}

    {/* 岗位推荐 */}
    {report.report_data.recommendations && (
      <div>
        <SectionTitle>岗位推荐</SectionTitle>
        <RecommendationsSection data={report.report_data.recommendations as any[]} />
      </div>
    )}

    {/* 行业趋势 */}
    {report.report_data.trend && (
      <div>
        <SectionTitle>行业趋势</SectionTitle>
        <TrendSection data={report.report_data.trend as any} />
      </div>
    )}

    {/* 发展路径 */}
    {report.report_data.path && (
      <div>
        <SectionTitle>发展路径</SectionTitle>
        <PathSection data={report.report_data.path as any} />
      </div>
    )}
  </div>
)}
```

- [ ] **Step 2: 新增 section 渲染组件（文件顶部）**

在 `ReportPage` 函数之前插入：

```tsx
function SectionTitle({ children }: { children: React.ReactNode }) {
  return <h3 className="text-white/50 font-semibold mb-3 text-lg">{children}</h3>;
}

function Badge({ children, color = "white/10" }: { children: React.ReactNode; color?: string }) {
  return <span className={`inline-block px-2.5 py-0.5 rounded-md bg-${color} border border-white/[0.06] text-white/45 text-xs`}>{children}</span>;
}

function PersonalitySection({ data }: { data: any }) {
  return (
    <div className="space-y-2 text-white/35 text-sm">
      <p>人格类型：<span className="text-white/65 font-semibold">{data.type}</span>（强度 {data.intensity}）</p>
      <div className="flex gap-4 text-xs text-white/25">
        <span>E-I: {data.ei}</span><span>S-N: {data.sn}</span><span>T-F: {data.tf}</span><span>J-P: {data.jp}</span>
      </div>
      {data.strengths?.length > 0 && <p>优势：{data.strengths.join("、")}</p>}
      {data.weaknesses?.length > 0 && <p>短板：{data.weaknesses.join("、")}</p>}
      {data.portrait && <p className="text-white/20 mt-2">{data.portrait}</p>}
    </div>
  );
}

function AbilitySection({ data }: { data: any }) {
  return (
    <div className="space-y-2 text-white/35 text-sm">
      <p>学历：<span className="text-white/65">{data.education}</span></p>
      <div className="flex gap-6">
        <span>知识评分：<span className="text-white/65">{data.knowledge}</span></span>
        <span>工具评分：<span className="text-white/65">{data.tool}</span></span>
        <span>项目评分：<span className="text-white/65">{data.project}</span></span>
      </div>
      <div className="flex gap-4 flex-wrap">
        <Badge>逻辑: {data.logic}</Badge>
        <Badge>沟通: {data.communication}</Badge>
        <Badge>证书竞赛: {data.cert}</Badge>
        <Badge>学习: {data.learning}</Badge>
      </div>
      {data.strengths?.length > 0 && <p>优势：{data.strengths.join("、")}</p>}
      {data.weaknesses?.length > 0 && <p>短板：{data.weaknesses.join("、")}</p>}
    </div>
  );
}

function RecommendationsSection({ data }: { data: any[] }) {
  return (
    <div className="space-y-2">
      {data.slice(0, 10).map((r: any, i: number) => (
        <div key={i} className="flex items-start gap-3 p-3 rounded-xl bg-white/[0.015] border border-white/[0.05]">
          <span className="text-white/20 text-xs font-mono mt-0.5 shrink-0">{i + 1}.</span>
          <div>
            <span className="text-white/55 text-sm">{r.reason?.slice(0, 150)}</span>
            <span className="text-white/20 text-xs ml-2">匹配度 {r.match_score} 分</span>
          </div>
        </div>
      ))}
    </div>
  );
}

function TrendSection({ data }: { data: any }) {
  const trends = data.trend_data?.trends || [];
  const warnings = data.risk_warnings || [];
  return (
    <div className="space-y-3">
      {trends.map((t: any, i: number) => (
        <div key={i} className="p-3 rounded-xl bg-white/[0.015] border border-white/[0.05]">
          <div className="flex items-center gap-2 mb-1">
            <span className="text-white/60 text-sm font-medium">{t.dimension}</span>
            <span className="text-white/15 text-xs">{t.score}分</span>
          </div>
          <p className="text-white/30 text-xs">{t.content}</p>
        </div>
      ))}
      {warnings.length > 0 && (
        <div className="p-3 rounded-xl bg-amber-500/[0.04] border border-amber-500/[0.1]">
          <p className="text-amber-400/70 text-xs font-medium mb-1">风险提示</p>
          <p className="text-amber-400/40 text-xs">{warnings.join("；")}</p>
        </div>
      )}
    </div>
  );
}

function PathSection({ data }: { data: any }) {
  const stages = [
    { label: "短期", key: "short", data: data.short, color: "border-emerald-500/[0.1] bg-emerald-500/[0.02]" },
    { label: "中期", key: "mid", data: data.mid, color: "border-blue-500/[0.1] bg-blue-500/[0.02]" },
    { label: "长期", key: "long", data: data.long, color: "border-purple-500/[0.1] bg-purple-500/[0.02]" },
  ];
  return (
    <div className="space-y-3">
      {stages.map((s) => {
        const d = s.data;
        if (!d) return null;
        return (
          <div key={s.key} className={`p-4 rounded-xl border ${s.color}`}>
            <h4 className="text-white/55 text-sm font-semibold mb-2">{s.label}（{d.duration}）</h4>
            <p className="text-white/35 text-sm mb-1">{d.goal}</p>
            {d.skills?.length > 0 && <p className="text-white/20 text-xs">技能：{d.skills.join("、")}</p>}
            {d.milestones?.length > 0 && <p className="text-white/20 text-xs">里程碑：{d.milestones.join("、")}</p>}
            {d.directions?.length > 0 && <p className="text-white/20 text-xs">方向：{d.directions.join("、")}</p>}
            {d.advanced_skills?.length > 0 && <p className="text-white/20 text-xs">高阶技能：{d.advanced_skills.join("、")}</p>}
          </div>
        );
      })}
    </div>
  );
}
```

- [ ] **Step 3: 修复下载按钮 — `.txt` → `.pdf`**

第 40 行：
```tsx
// 改前
a.download = `report_v${report.version}.txt`;
// 改后
a.download = `report_v${report.version}.pdf`;
```

- [ ] **Step 4: Commit**

```bash
git add frontend/src/pages/dashboard/Report.tsx
git commit -m "fix: 报告页结构化渲染替代原始JSON dump，下载后缀改为.pdf"
```

---

### Task 7: 后端 PDF 生成也改为人类可读格式

**文件:** `backend/app/services/report_service.py:93-158`

**问题:** `_generate_pdf` 第 143 行用 `json.dumps(content)` 把每个 section 写成 JSON，下载的 PDF 也是原始 JSON。

**改法:** 在 `_generate_pdf` 中复用人类可读的格式化文本替代 `json.dumps`。

- [ ] **Step 1: 新增格式化辅助函数**

在 `_generate_pdf` 前面（约第 92 行前）插入：

```python
def _fmt_personality(p: dict | None) -> str:
    if not p: return "暂无数据"
    return (
        f"人格类型：{p.get('type', '')}（强度 {p.get('intensity', '')}）\n"
        f"EI={p.get('ei', '')}  SN={p.get('sn', '')}  TF={p.get('tf', '')}  JP={p.get('jp', '')}\n"
        f"优势：{', '.join(p.get('strengths', []))}\n"
        f"短板：{', '.join(p.get('weaknesses', []))}\n"
        f"描述：{p.get('portrait', '')}"
    )

def _fmt_ability(a: dict | None) -> str:
    if not a: return "暂无数据"
    return (
        f"学历：{a.get('education', '')}\n"
        f"知识={a.get('knowledge', '')}  工具={a.get('tool', '')}  项目={a.get('project', '')}\n"
        f"逻辑={a.get('logic', '')}  沟通={a.get('communication', '')}  证书竞赛={a.get('cert', '')}  学习={a.get('learning', '')}\n"
        f"优势：{', '.join(a.get('strengths', []))}\n"
        f"短板：{', '.join(a.get('weaknesses', []))}"
    )

def _fmt_recommendations(recs: list | None) -> str:
    if not recs: return "暂无数据"
    lines = []
    for i, r in enumerate(recs[:10], 1):
        lines.append(f"{i}. 匹配度 {r.get('match_score', 0)}分 — {r.get('reason', '')[:120]}")
    return "\n".join(lines)

def _fmt_trend(t: dict | None) -> str:
    if not t: return "暂无数据"
    lines = []
    trends = (t.get("trend_data") or {}).get("trends", []) if isinstance(t.get("trend_data"), dict) else []
    for item in trends[:6]:
        lines.append(f"【{item.get('dimension', '')}】{item.get('content', '')}")
    if t.get("risk_warnings"):
        lines.append(f"\n风险提示：{'；'.join(t['risk_warnings'][:5])}")
    return "\n".join(lines) if lines else "暂无趋势数据"

def _fmt_path(dp: dict | None) -> str:
    if not dp: return "暂无数据"
    parts = []
    for label, key in [("短期", "short"), ("中期", "mid"), ("长期", "long")]:
        d = dp.get(key, {})
        if d:
            parts.append(f"【{label}】{d.get('duration', '')}\n目标：{d.get('goal', '')}\n技能：{', '.join(d.get('skills', []))}")
            if d.get('milestones'):
                parts[-1] += f"\n里程碑：{', '.join(d['milestones'])}"
            if d.get('directions'):
                parts[-1] += f"\n方向：{', '.join(d['directions'])}"
            if d.get('advanced_skills'):
                parts[-1] += f"\n高阶技能：{', '.join(d['advanced_skills'])}"
    return "\n\n".join(parts) if parts else "暂无发展路径数据"
```

- [ ] **Step 2: 修改 _generate_pdf 中的 section 渲染**

将第 128-150 行改为：

```python
        sections = [
            ("性格画像", _fmt_personality(data.get("personality"))),
            ("能力画像", _fmt_ability(data.get("ability"))),
            ("岗位推荐", _fmt_recommendations(data.get("recommendations"))),
            ("行业趋势", _fmt_trend(data.get("trend"))),
            ("发展路径", _fmt_path(data.get("path"))),
        ]

        for title, text in sections:
            if y < 100:
                c.showPage()
                if font_registered: c.setFont("ChineseFont", 12)
                else: c.setFont("Helvetica", 10)
                y = 800
            c.drawString(50, y, f"--- {title} ---"); y -= 20
            for line in text.split("\n"):
                if y < 50:
                    c.showPage()
                    if font_registered: c.setFont("ChineseFont", 12)
                    else: c.setFont("Helvetica", 10)
                    y = 800
                c.drawString(60, y, line[:120]); y -= 16
            y -= 10
```

- [ ] **Step 3: 移除不再需要的 json import（如果 json 在其他地方仍使用则保留）**

`_generate_pdf` 不再用 `json`，但文件其他地方 (`_rec`, `_trend`, `_path` 等) 没有用到 `json`。实际上第 96 行的 `import json` 在整个文件中未使用（之前的 `json.dumps` 已经在 PDF 里），可以删掉。

- [ ] **Step 4: 验证格式化函数**

```bash
cd backend && python -c "
from app.services.report_service import _fmt_personality, _fmt_ability, _fmt_recommendations
p = _fmt_personality({'type':'ESTJ','intensity':3,'ei':10,'sn':10,'tf':10,'jp':10,'strengths':['逻辑'],'weaknesses':['情感'],'portrait':'管理者'})
assert 'ESTJ' in p, f'fail: {p}'
a = _fmt_ability({'education':'硕士','knowledge':70,'tool':82,'project':45,'logic':'较强','communication':'中等','cert':'突出','learning':'良好','strengths':['Python'],'weaknesses':[]})
assert '硕士' in a, f'fail: {a}'
r = _fmt_recommendations([{'match_score':68.3,'reason':'React经验符合需求'}])
assert '68.3' in r, f'fail: {r}'
print('All PDF format checks passed')
"
```

预期：`All PDF format checks passed`

- [ ] **Step 5: Commit**

```bash
git add backend/app/services/report_service.py
git commit -m "fix: PDF 生成使用人类可读格式替代 json.dumps"
```

---

## 验证清单（全部完成后）

1. 列表接口 `POST /recommend` 返回的 `match_score` 正常（embedding 分）
2. 详情接口 `GET /positions/{id}/detail` 的 `match_analysis.overall_match_score` 与 `embedding_match_score` 一致
3. 推荐结果中同 `(title, city)` 岗位不重复
4. 推荐耗时显著下降（批量调用代替 10 次并行调用）
5. 发展路径内容体现用户画像（学历、技能、优劣势）
6. 趋势资源 URL 无编造（留空或真实链接）
7. **前端报告页结构化渲染**（性格卡片、能力标签、推荐列表、趋势卡片、路径时间轴）
8. **下载 PDF 内容人类可读**，无原始 JSON
